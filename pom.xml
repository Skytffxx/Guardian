<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.skyzzz.guardian</groupId>
  <artifactId>guardian-parent</artifactId>
  <version>1.0.1</version>
  <packaging>pom</packaging>

  <name>Guardian</name>
  <description>Crossplay-aware, low-false-positive anticheat for Paper/Spigot</description>
  <url>https://github.com/skyzzz/guardian</url>

  <modules>
    <module>guardian-api</module>
    <module>guardian-checks</module>
    <module>guardian-core</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>

    <!-- Native build target: 1.21.11. Compiles against the Paper API; runs 1.21.11 → 26.x+ -->
    <paper.version>1.21.11-R0.1-SNAPSHOT</paper.version>
    <packetevents.version>2.7.0</packetevents.version>
    <vault.version>1.7</vault.version>
    <luckperms.version>5.4</luckperms.version>
    <placeholderapi.version>2.11.6</placeholderapi.version>
    <bstats.version>3.0.2</bstats.version>
    <hikari.version>5.1.0</hikari.version>
    <sqlite.version>3.46.0.0</sqlite.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
    <repository>
      <id>codemc</id>
      <url>https://repo.codemc.io/repository/maven-public/</url>
    </repository>
    <repository>
      <id>placeholderapi</id>
      <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
    </repository>
    <repository>
      <id>luckperms</id>
      <url>https://repo.luckperms.net/</url>
    </repository>
    <repository>
      <id>jitpack</id>
      <url>https://jitpack.io</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <!-- Internal modules -->
      <dependency>
        <groupId>com.skyzzz.guardian</groupId>
        <artifactId>guardian-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.skyzzz.guardian</groupId>
        <artifactId>guardian-checks</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Server API -->
      <dependency>
        <groupId>io.papermc.paper</groupId>
        <artifactId>paper-api</artifactId>
        <version>${paper.version}</version>
        <scope>provided</scope>
      </dependency>

      <!-- Multi-version packet wrappers (through 26.2+) -->
      <dependency>
        <groupId>com.github.retrooper</groupId>
        <artifactId>packetevents-spigot</artifactId>
        <version>${packetevents.version}</version>
        <scope>provided</scope>
      </dependency>

      <!-- Optional integrations, all provided -->
      <dependency>
        <groupId>com.github.MilkBowl</groupId>
        <artifactId>VaultAPI</artifactId>
        <version>${vault.version}</version>
        <scope>provided</scope>
        <exclusions>
          <exclusion>
            <groupId>org.bukkit</groupId>
            <artifactId>bukkit</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
      <dependency>
        <groupId>net.luckperms</groupId>
        <artifactId>api</artifactId>
        <version>${luckperms.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>me.clip</groupId>
        <artifactId>placeholderapi</artifactId>
        <version>${placeholderapi.version}</version>
        <scope>provided</scope>
      </dependency>

      <!-- Shaded -->
      <dependency>
        <groupId>org.bstats</groupId>
        <artifactId>bstats-bukkit</artifactId>
        <version>${bstats.version}</version>
        <scope>compile</scope>
      </dependency>
      <dependency>
        <groupId>com.zaxxer</groupId>
        <artifactId>HikariCP</artifactId>
        <version>${hikari.version}</version>
        <scope>compile</scope>
      </dependency>
      <dependency>
        <groupId>org.xerial</groupId>
        <artifactId>sqlite-jdbc</artifactId>
        <version>${sqlite.version}</version>
        <scope>compile</scope>
      </dependency>

      <!-- Test -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>5.10.2</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.2.5</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.0</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <!-- -Pjava25 for the 26.1+ runtime requirement -->
    <profile>
      <id>java25</id>
      <properties>
        <maven.compiler.release>25</maven.compiler.release>
      </properties>
    </profile>
  </profiles>
</project>